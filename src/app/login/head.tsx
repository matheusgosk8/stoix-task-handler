import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'stoix-task-handler - login',
  description: 'Página de login',
  icons: {
    icon: '/favicon.ico', 
  },
};

export default function Head() {
  return null; 
}